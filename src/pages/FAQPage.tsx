import styled from 'styled-components';

export default function FAQPage() {
    return <PageWrapper>
        준비 중
    </PageWrapper>;
}

const PageWrapper = styled.div`
    width: 400px;
    height: 100%;
    
`